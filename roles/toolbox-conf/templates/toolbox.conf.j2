server {
        listen 80;
        listen [::]:80;

        root {{ toolbox_dir }};
        index index.html index.htm index.nginx-debian.html;

        server_name {{ toolbox_fqdn }} www.{{ toolbox_fqdn }};

        location / {
                try_files $uri $uri/ =404;
        }
}
